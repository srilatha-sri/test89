name: check 

on:
 workflow_dispatch:
 

jobs:
  commithistory:
    name: action1
    runs-on: [ubuntu-latest]
    steps: 
      - uses: actions/checkout@v3
      - name: abc
        id: checkc
        run: |-
          if [[ $(git log -1 | grep tgh) ]]
          then
            echo ***commit history contains int1***
          else
            exit 1
          fi
    
